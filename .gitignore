# 빌드 산출물
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo

# IDE
.vs/
.idea/
*.userprefs

# 로컬 설정 (혹시라도 프로젝트 안에 생길 경우)
settings.json
last-usage.json

# OS
Thumbs.db
.DS_Store

# 릴리스에 첨부할 배포본 (저장소에는 넣지 않는다)
/AiQuotaTray.exe
/AiQuotaTray-standalone.exe

# 배포본을 모아두는 곳. 릴리스에 올릴 exe를 여기서 꺼내 쓴다.
/dist/
/artifacts/
/launcher/target/

# 작업용 메모. 저장소에는 올리지 않는다.
/CLAUDE.md

# Windows가 폴더에 남기는 파일
desktop.ini
