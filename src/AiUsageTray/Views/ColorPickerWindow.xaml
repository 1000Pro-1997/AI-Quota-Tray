<Window x:Class="AiUsageTray.Views.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        SizeToContent="WidthAndHeight"
        WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True"
        AllowsTransparency="True" Background="Transparent"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>
        <!-- 팔레트 한 칸. 고른 색에는 테두리가 생긴다. -->
        <Style x:Key="Chip" TargetType="RadioButton">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <!-- 선택 표시용 바깥 링 -->
                            <Border x:Name="Ring" CornerRadius="8"
                                    BorderThickness="2" BorderBrush="Transparent"/>
                            <Border x:Name="Swatch" CornerRadius="6" Margin="3"
                                    Background="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Ring" Property="BorderBrush"
                                        Value="{DynamicResource BorderBrush2}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Ring" Property="BorderBrush"
                                        Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FooterButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="6" Padding="{TemplateBinding Padding}"
                                Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource HoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- 그림자를 위해 여백을 두고 카드를 그린다. -->
    <Border Margin="10" CornerRadius="10" Padding="12,12,12,8"
            Background="{DynamicResource CardBrush}"
            BorderBrush="{DynamicResource BorderBrush2}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Direction="270"
                              Opacity="0.28" Color="Black"/>
        </Border.Effect>

        <StackPanel>
            <TextBlock x:Name="TitleText" FontSize="11.5" FontWeight="SemiBold"
                       Foreground="{DynamicResource SubtleBrush}" Margin="2,0,0,8"/>

            <!-- 12색: 4열 3행 -->
            <UniformGrid x:Name="Palette" Columns="4" Rows="3"/>

            <Border Height="1" Background="{DynamicResource BorderBrush2}" Margin="0,9,0,5"/>

            <Grid>
                <Button x:Name="CustomButton" Style="{StaticResource FooterButton}"
                        HorizontalAlignment="Left" Click="OnCustom"/>
                <Button x:Name="ResetButton" Style="{StaticResource FooterButton}"
                        HorizontalAlignment="Right" Click="OnReset"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
